# AI 관상소 — 프로젝트 정리 문서

이직 포트폴리오용 backoffice 프로젝트와는 별개로 진행하는 사이드 프로젝트.
목적: **부수익보다 재미/AI API 실습 위주.** 광고·결제 등 수익화는 하지 않기로 결정(3번 참고).

---

## 0. [2026-08-26] 대규모 개편 요약

한 번에 4가지 요청 반영:
1. **UI 전면 리뉴얼** — "고서/한지" 톤 → "홀로그램 포춘카드" 톤 (아래 0-1 참고)
2. **공유 링크로 들어온 사람의 이동 동선 개선** — 상단 고정 네비 + 결과 화면 하단 "다른 기능 바로가기" 칩 추가 (아래 0-2)
3. **신규 기능 추가** — 🌟 오늘의 운세(1인, 생년월일만으로 총운/재물운/연애운/행운아이템) (아래 0-3)
4. **파일 구조 리팩토링** — 단일 HTML 파일 → HTML/CSS/JS(ES 모듈) 분리 (아래 0-4)

덤으로: 카톡 공유 링크가 텍스트로만 표시되던 버그(인코딩 이중 이스케이프로 URL이 불필요하게 부풀던 문제)도 같이 수정.

### 0-1. 새 디자인 컨셉 — "홀로그램 포춘카드"
기존 고서/두루마리 톤(크림+테라코타 계열)은 사실 AI가 가장 흔하게 뽑는 톤이라 "트렌디함"과는 결이 달라서 완전히 새로 잡음. 요즘 실제 한국 사주/타로 앱들이 쓰는 신비로운 딥퍼플 배경 + K-포토카드 스티커 감성을 참고.

- **컬러**: 배경은 딥 인디고~블랙(`#0e0a1f`) 위에 보라/핑크 글로우. 포인트는 보라(`#8b5cf6`)·핑크(`#f472b6`)·앰버(`#fbbf24`)·민트(`#34d9c5`) 4색을 도는 홀로그램 conic-gradient — 도장(인장)을 "홀로그램 스티커 배지"로 재해석해서 반복 사용(로고 orb, 로딩 스피너, 결과 스코어 스티커에 전부 동일 그라데이션 적용 = 시그니처 요소).
- **타이포**: 제목/버튼은 'Do Hyeon'(볼드하고 발랄한 한글 디스플레이 서체), 본문은 'Pretendard'.
- **디테일**: 유리질감(backdrop-blur) 카드, 알약형(pill) 그라데이션 버튼, 포커스 시 outline 명시(키보드 접근성), `prefers-reduced-motion` 대응(스피너/orb 회전 정지).
- 톤/컬러 토큰은 전부 `styles.css` 상단 `:root` 변수로 관리 — 다음에 색 하나만 바꾸고 싶으면 거기만 건드리면 됨.

### 0-2. 공유 링크 진입 시 이동 동선 개선
기존 문제: 공유된 결과 링크로 들어온 사람은 "다른 사진으로 다시 보기" 버튼밖에 없어서 홈으로도, 다른 기능(궁합 등)으로도 못 넘어갔음.

- **상단 고정 네비바** 추가 — 모든 화면(결과 화면 포함)에서 로고 클릭 시 항상 홈으로 이동 가능.
- 결과 화면 하단에 **"처음으로" 버튼**을 "다시 해보기"와 나란히 배치.
- 결과 화면 하단에 **다른 기능 바로가기 칩**(예: 관상 결과를 보고 있으면 "💕 궁합", "🌟 오늘의 운세" 칩이 뜸) 추가 — 공유 링크로 들어온 사람이 바로 다른 기능을 체험할 수 있게 해서 바이럴 루프 강화 목적도 겸함.

### 0-3. 신규 기능 — 🌟 오늘의 운세 (1인 사주)
- 입력: 이름(선택) + 생년월일(필수) + 태어난 시각(선택) — 사진도, 상대방 정보도 필요 없어서 궁합보다 진입장벽이 더 낮음(공유받은 사람이 바로 시도해보기 좋은 "미끼" 기능으로 설계).
- 결과: 총운 / 재물운 / 연애운 / 행운 아이템 4개 카드 + 오늘의 총운 점수 + 총평. 서버 프롬프트(`api/analyze.js`의 `UNSE_SYSTEM_PROMPT`)에 **한국 시간 기준 오늘 날짜**를 함께 넘겨서, 같은 생년월일이라도 날짜가 바뀌면 결과가 달라지도록 설계(재방문 유도).
- 관상/궁합과 마찬가지로 결과 형태(제목+features+점수+총평)가 동일해서, 프론트에서 결과 렌더링 함수(`render.js`)를 3개 기능이 공용으로 씀.

### 0-4. 파일 구조 리팩토링
단일 `ai-gwansang.html`(HTML+CSS+JS 전부)이 계속 길어지는 문제 해결. 빌드 과정 없이 즉시 배포 가능하다는 기존 장점은 그대로 유지하면서(번들러 없음), **네이티브 ES 모듈**(`<script type="module">`)로 파일만 분리:

```
ai-gwansang.html   구조(뼈대)만 남음, 246줄
styles.css         디자인 시스템 전체, 384줄
js/
  dom-utils.js     생년월일/시진 드롭다운, 화면전환(showView) — 104줄
  share.js         공유 링크 인코딩/디코딩, 공유 액션 — 57줄
  api.js           /api/analyze 호출 래퍼(관상/궁합/운세 3종) — 34줄
  render.js         결과 화면 렌더러(3기능 공용, 리팩토링 핵심) — 64줄
  main.js           진입점, 기능별 설정(TYPE_CONFIG)과 이벤트 바인딩 — 450줄
```
- 관상/궁합/오늘의 운세는 결과 형태가 동일해서(제목+features[]+점수+총평), `main.js`의 `TYPE_CONFIG` 객체 하나에 기능별 설정(로딩 문구, 점수 라벨, 공유 문구, 폼 화면 id 등)을 몰아넣고, `#result-view` DOM 하나 + `render.js`의 `renderResult()` 하나를 공용으로 씀. 예전엔 결과 화면 자체가 `#result-view`/`#couple-result-view` 2개로 중복돼 있었음.
- 브라우저가 ES 모듈을 네이티브 지원해서 번들러(webpack/vite 등) 도입 없이도 파일 분리가 가능함 — Vercel은 그냥 정적 파일로 서빙하면 끝.
- `vercel.json`은 변경 없음(루트 `/` → `ai-gwansang.html` rewrite만 있으면 되고, `styles.css`/`js/*.js`는 상대경로라 자동으로 잘 찾아감).

### 0-5. [부수 수정] 카톡 공유 링크가 텍스트로만 뜨던 버그
`LZString.compressToEncodedURIComponent()` 결과 문자열에 섞인 `+`,`$` 문자가 `URLSearchParams.set()`을 거치며 `%2B`/`%24`로 다시 인코딩되어 URL이 불필요하게 부풀었고(스샷에서 확인), 카톡이 너무 길고 복잡한 URL의 미리보기 생성을 포기하고 텍스트로만 표시한 것으로 추정.
→ **base64 → base64url**(`+`/`/`/`=` 를 `-`/`_`/제거로 치환)로 인코딩 방식을 바꿔서, `URLSearchParams`가 다시 손댈 문자가 아예 없게 수정 (`share.js`).

**⚠️ 주의: 이 변경으로 기존에 이미 공유했던 링크(구버전 인코딩)는 새 배포 이후 깨짐** — `decodeShareData`가 새 방식으로만 디코딩을 시도하기 때문. try/catch로 조용히 홈 화면으로 빠지긴 하지만, 이미 뿌린 링크가 있다면 참고할 것.

---

## 1. 컨셉

- 사진 업로드 → AI가 전통 관상가 컨셉으로 재미있게(과장·유쾌 톤) 관상을 풀이
- 생년월일 2인 입력 → AI가 사주명리 컨셉으로 궁합을 풀이
- 생년월일 1인 입력 → AI가 오늘 하루의 총운/재물운/연애운/행운 아이템을 풀이 **(신규)**
- 100% 오락 목적, 과학적 근거 없음을 명시적으로 고지

## 2. 화면 구조

```
홈 화면 (메뉴 카드 3개)
├── 🔮 관상 보기
│     사진 업로드(필수) + 이름/생년월일/태어난시각/태어난장소(전부 선택, 접이식)
│
├── 💕 사주 궁합 보기
│     나(이름 선택/생년월일 필수/시각 선택) + 상대방(동일)
│
└── 🌟 오늘의 운세 보기 (신규)
      이름(선택) + 생년월일(필수) + 시각(선택)

→ 세 기능 모두 결과는 동일한 #result-view 하나를 공용으로 사용
   (제목 + features 카드들 + 점수 + 총평 + 공유/재시도/다른기능 바로가기)
```

- 탭 방식이 아니라 **홈 화면에서 카드 선택 → 진입 → 뒤로가기**로 이동하는 구조 유지
- 모든 화면에서 상단 고정 네비(로고 클릭)로 언제든 홈 복귀 가능 **(신규)**

## 3. 주요 설계 결정과 이유

### 3-1. 수익화 안 함 (광고/결제 보류)
(기존과 동일 — 변경 없음. 원문 유지)

### 3-2. 궁합을 "사진 2장"에서 "생년월일 2명"으로 전환
(기존과 동일 — 변경 없음)

### 3-3. 관상 프롬프트 — 실제 관상학 이론 반영
(기존과 동일 — 변경 없음)

### 3-4. "항상 긍정적"에서 "다양한 톤"으로 개선
(기존과 동일 — 변경 없음. 오늘의 운세 프롬프트도 동일 원칙 적용)

### 3-5. 오늘의 운세를 "가장 진입장벽 낮은 기능"으로 설계 (신규)
- 사진도, 상대방 정보도 필요 없이 생년월일 하나만 있으면 됨 → 공유 링크로 들어온 사람이 그 자리에서 바로 시도해볼 수 있는 "미끼" 기능으로 의도적 설계.
- 서버에 오늘 날짜(KST)를 함께 넘겨서 매일 다른 결과가 나오게 함 — 재방문/재공유 유도.

## 4. 기술 스택 / 비용 구조

### 4-1. 프론트엔드: 순수 HTML/CSS/JS + ES 모듈로 파일 분리 (2026-08-26 리팩토링)
- 프레임워크/번들러 없음 원칙은 유지, 대신 단일 파일이 계속 길어지는 문제를 ES 모듈(`<script type="module">`)로 해결. 상세 구조는 0-4 참고.

### 4-2. 백엔드: Vercel Serverless Function (프록시)
- 파일: `api/analyze.js` — `type` 값(`gwansang`/`gunghap`/`unse`)에 따라 시스템 프롬프트·유저 텍스트·요청 바디 검증을 분기
- 역할: (1) API 키 서버 보관 (2) 하루 호출 횟수 캡 (3) Gemini/Claude 라우팅

### 4-3. AI 모델 — Gemini 우선, Claude 폴백
(기존과 동일 — 변경 없음. 오늘의 운세도 동일 라우팅 방식 사용)

### 4-4. 비용 안전장치
(기존과 동일)

### 4-5~4-9. UI/공유 링크/카톡/개인정보 이슈
0번 섹션에 이번 세션 변경사항 반영해서 재정리함. 세부 히스토리(생년월일 드롭다운, iOS 줌 버그, 공유 링크 URL 인코딩 방식 등)는 이전 버전 문서 참고.

## 5. 개인정보 / 법적 이슈
(기존과 동일 — 변경 없음. 오늘의 운세 기능도 동일한 동의 체크박스 하드 게이트 방식 적용됨)

## 6. 파일 목록

| 파일 | 역할 |
|---|---|
| `ai-gwansang.html` | HTML 구조(뼈대)만 포함. CSS/JS는 외부 파일로 분리 |
| `styles.css` | 디자인 시스템 전체("홀로그램 포춘카드" 컨셉) |
| `js/dom-utils.js` | 생년월일/시진 드롭다운 유틸, 화면 전환(`showView`) |
| `js/share.js` | 공유 링크 인코딩/디코딩(base64url), 공유 액션(OS 공유시트→카카오 SDK→클립보드) |
| `js/api.js` | `/api/analyze` 호출 래퍼 3종 |
| `js/render.js` | 결과 화면 공용 렌더러(관상/궁합/운세) |
| `js/main.js` | 진입점. 기능별 설정(`TYPE_CONFIG`) + 이벤트 바인딩 |
| `api/analyze.js` | 서버 프록시. `gwansang`/`gunghap`/`unse` 타입 라우팅, 하루 호출 캡 |
| `vercel.json` | 루트(`/`) → `ai-gwansang.html` rewrite (변경 없음) |
| `og-image.png` | 카톡/SNS 공유 썸네일(1200x630). 리뉴얼된 톤에 맞게 재제작 필요 여부 검토할 것 |
| `CLAUDE.md` | 이 문서 |

## 6-1. 배포 현황
- 배포: `https://ai-gwansang-one.vercel.app` (Vercel + GitHub 연동)
- **[확인 필요]** 새 파일 구조(`styles.css`, `js/*.js`)가 레포 루트/`js` 경로 그대로 올라가는지 push 후 확인할 것

## 7. 다음 할 일

- [x] UI 전면 리뉴얼 ("홀로그램 포춘카드" 컨셉)
- [x] 공유 링크 진입 시 홈/다른기능 이동 동선 추가
- [x] 오늘의 운세(1인 사주) 기능 추가
- [x] 단일 파일 → HTML/CSS/JS(ES 모듈) 리팩토링
- [x] 카톡 공유 링크 텍스트로만 뜨던 버그 수정 (base64url 인코딩)
- [ ] **og-image.png를 새 홀로그램 톤에 맞게 재제작할지 결정** (기존 고서 톤 이미지가 새 UI와 안 어울릴 수 있음)
- [ ] 실제 배포 후 카톡 링크 미리보기 재테스트 (카카오 공유 디버거로 캐시 초기화 필요할 수 있음: developers.kakao.com/tool/clear/og)
- [ ] 기존에 이미 공유된 구버전 링크가 깨지는 것에 대한 안내가 필요한지 판단 (트래픽 적으면 무시 가능)
- [ ] 카카오 공유 SDK 연동 (`YOUR_KAKAO_JS_KEY` 교체) — 여전히 선택사항, 등록 안 해도 OG 미리보기 자체는 정상 작동
- [ ] 개인정보처리방침 페이지 실제 작성
- [ ] (선택) Vercel KV 연결해서 하루 카운터 영속화

## 8. 진행 방식 메모
(기존과 동일 — 변경 없음)
