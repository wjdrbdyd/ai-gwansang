# AI 관상소 — 프로젝트 정리 문서

이직 포트폴리오용 backoffice 프로젝트와는 별개로 진행하는 3일짜리 사이드 프로젝트.
목적: **부수익보다 재미/AI API 실습 위주.** 광고·결제 등 수익화는 하지 않기로 결정(아래 3번 참고).

---

## 1. 컨셉

- 사진 업로드 → AI가 전통 관상가 컨셉으로 재미있게(과장·유쾌 톤) 관상을 풀이
- 생년월일 2인 입력 → AI가 사주명리 컨셉으로 궁합을 풀이
- 100% 오락 목적, 과학적 근거 없음을 명시적으로 고지

## 2. 화면 구조

```
홈 화면 (메뉴 카드 2개)
├── 🔮 관상 보기
│     사진 업로드(필수) + 이름/생년월일/태어난시각/태어난장소(전부 선택, 접이식)
│     → 결과: 부위별(이마/눈/코/입) 풀이 + 오늘의 총운 점수
│
└── 💕 사주 궁합 보기
      나(이름 선택/생년월일 필수/시각 선택) + 상대방(동일)
      → 결과: 케미포인트/다툼포인트/궁합조언 + 궁합지수
```

- 탭 방식이 아니라 **홈 화면에서 카드 선택 → 진입 → 뒤로가기**로 이동하는 구조로 확정 (탭보다 각 기능이 독립된 느낌을 주기 위함)

## 3. 주요 설계 결정과 이유

### 3-1. 수익화 안 함 (광고/결제 보류)
- 검토했던 옵션: 배너광고, 강제시청 영상광고, 즉시승인 네트워크(Adsterra 등), 건당 결제, 쿠팡파트너스
- 전부 기각한 이유:
  - 광고/제휴는 트래픽 절대량에 비례 → 트래픽 예측 불가능한 상태에서 무의미
  - 즉시승인 광고 네트워크는 UX를 해치는 포맷(팝언더 등) 위주라 이 앱의 성장동력(바이럴 공유)을 오히려 깎아먹음
  - "재미 콘텐츠에 결제"는 실사용 전환 기대하기 어려움 (사용자 본인 지적)
  - Vercel Hobby 플랜도 상업적 이용(광고 포함) 시 Pro($20/월) 필요 — 광고 안 붙이면 이 비용도 안 듦
- **결론: API 비용은 본인이 부담하는 학습/재미 프로젝트로 스코프 확정.** 대신 아래 4번처럼 원가를 최대한 낮춰서 리스크 최소화.

### 3-2. 궁합을 "사진 2장"에서 "생년월일 2명"으로 전환
- 최초 설계: 상대방 사진도 업로드해서 "얼굴 궁합" 컨셉
- 문제 발견: 업로드하는 사람은 동의했다 쳐도, **사진 속 상대방(제3자)은 자기 얼굴이 AI로 분석되는 것에 동의한 적 없음** — 몰래 업로드 가능성, 개인정보(얼굴) 이슈
- 대안 검토: 동의 체크박스만 추가 → 근본 해결 안 됨(검증 불가)
- **최종 결정: 사주궁합(생년월일 기반)으로 전면 전환**
  - 제3자 동의 이슈 원천 해소 (생년월일은 얼굴 사진보다 훨씬 가벼운 정보, "친구야 생일 알려줘"는 일상적 요청)
  - 오히려 "얼굴 궁합"은 전통적으로 존재하지 않는 개념인 반면, **사주궁합은 실제 오래된 전통 개념**이라 "실제 이론 반영" 요구사항에도 더 부합

### 3-3. 관상 프롬프트 — "완전 창작"에서 "실제 관상학 이론 반영"으로 개선
- 최초: "완전히 지어낸 재미용 관상 풀이" — AI가 근거 없이 아무 말이나 생성
- 문제 제기: 실제 관상학 용어/개념을 안 쓰니 그럴듯함이 떨어짐
- 개선: 실제 관상학의 부위별 해석 틀(이마=초년운/관운, 눈썹=인복, 코=재물운, 입=식복/애정운, 턱=말년운) 명시적으로 프롬프트에 반영. 사주궁합도 동일하게 오행 상생상극/십이지 띠궁합/음양조화 개념 반영
- 단, 이론을 "빌려 쓰되" 내용 자체는 여전히 창작 — 진지한 진단으로 오인 안 되게 안전장치 유지

### 3-4. "항상 긍정적"에서 "다양한 톤(귀여운 불운 포함)"으로 개선
- 문제 제기: 결과가 항상 좋게만 나오니 예측 가능해지고 재미가 떨어짐
- 개선: 과장된 행운·황당한 불운·엉뚱한 반전을 예측불가능하게 섞도록 지시
- 안전장치: "불운"도 귀엽고 웃긴 소소한 해프닝 수준으로 제한(예: 지갑 어디 뒀는지 까먹을 상). 외모평가/성격비하/나이·인종·성별 추측은 여전히 금지

## 4. 기술 스택 / 비용 구조

### 4-1. 프론트엔드: 순수 HTML/CSS/JS (프레임워크 없음)
- React/Vue 대신 순수 HTML을 선택한 이유: 빌드 과정 없이 즉시 테스트 가능, 배포 단순(파일 하나), 이 정도 규모(상태 몇 개, 화면 전환만 있음)에선 프레임워크 이점이 안 살아남
- 파일: `ai-gwansang.html`

### 4-2. 백엔드: Vercel Serverless Function (프록시)
- 브라우저에서 AI API를 직접 호출하면 API 키가 그대로 노출됨 → 반드시 서버(프록시) 경유 필요
- 파일: `api-analyze.js` — 배포 시 Vercel 프로젝트의 `api/analyze.js` 경로에 위치
- 역할: (1) API 키를 서버에서만 보관 (2) 하루 호출 횟수 캡으로 예상 밖 과금 방지 (3) Gemini/Claude 라우팅
- **[완료] `ai-gwansang.html`의 fetch 주소를 `/api/analyze`로 교체 완료.** 관상(`analyzeFace`)/궁합(`analyzeCompatibility`) 함수 둘 다 프록시 호출 방식으로 전환됨. 프론트는 이제 시스템 프롬프트를 직접 안 갖고 있고(서버가 관리), 서버가 이미 파싱된 JSON 결과를 그대로 반환하므로 프론트에서 텍스트 블록 파싱 로직 불필요.

### 4-3. AI 모델 — Gemini 우선, Claude 폴백
- **[2026-08-24 업데이트] Google 공식 pricing 페이지(ai.google.dev/gemini-api/docs/pricing) 확인 결과, `gemini-2.5-flash`는 입력/출력 토큰 모두 무료 등급에서 "Free of charge"임을 재확인.** 즉 하루 캡(50회) 안에서는 Gemini 쪽 비용이 사실상 $0에 가까움. 아래 원가 표는 최초 검토 당시(2026-08) 대략치로, 실제로는 Gemini 무료 등급 덕분에 더 낮음.
- 비교 검토 결과(2026-08 기준 대략치, 실제 배포 시 최신 가격 재확인 필요):
  | 모델 | 1회 호출당 원가(대략) |
  |---|---|
  | Claude Sonnet | ~15~20원 |
  | Claude Haiku | ~5~7원 |
  | Gemini Flash | 무료 등급 사용 시 $0 (2026-08-24 공식 확인) |
- 최초 "랜덤 분산(Claude/Gemini 70:30)" 시도 → 회사마다 톤이 미묘하게 달라 결과 일관성이 깨지는 문제 발견 → **"Gemini 기본 사용, 장애시에만 Claude 폴백"**으로 변경 확정
- 원가가 이미 매우 낮아서(Gemini는 무료 등급, Claude 폴백만 소액 발생) 추가 절감의 실익보다 톤 일관성을 우선함
- **주의**: 정확한 RPM/RPD(분당·일일 호출 한도)는 계정별로 달라질 수 있어 aistudio.google.com 콘솔에서 직접 확인 필요. 무료 등급 한도 초과 시 자동으로 에러 발생 → 서버의 `DAILY_LIMIT` 캡이 이 한도보다 낮게 설정돼 있는지 배포 전 재확인할 것

### 4-4. 비용 안전장치
- Anthropic/Google 콘솔에서 지출 하드리밋 설정 (예: 월 $5)
- 서버 프록시에 하루 전체 호출 50회 캡 (`DAILY_LIMIT` 상수로 조정 가능)
- 주의: 카운터는 메모리 변수라 서버리스 함수 특성상 재배포/인스턴스 재시작 시 리셋됨 — 정확한 카운팅 필요시 Vercel KV 등 영속 저장소 연결 필요 (현재 미연결, 주석으로 표시해둠)

### 4-5. UI/UX 개선 이력
- **생년월일 입력을 `<input type="date">`에서 연/월/일 드롭다운 3개로 전환**: 모바일 네이티브 날짜 캘린더가 "생년월일처럼 수십 년 전 날짜를 골라야 하는 경우"에 UX가 나쁨(월 단위로 한 장씩 넘겨야 함) → 연/월/일 각각 `<select>`로 분리. 월/연도 바뀌면 일(day) 옵션이 자동으로 맞춰서 갱신(예: 2월이면 29일까지만). 관상 화면 선택 입력 + 궁합 화면 나/상대방 총 3곳 모두 적용. 관련 헬퍼 함수: `populateYMDSelect`, `getYMDValue`, `resetYMDValue`.
- **태어난 시각 입력을 `<input type="time">`에서 십이시진 드롭다운으로 전환**: 분 단위 시간 휠피커가 모바일에서 불편했던 문제 → 사주에서 실제로 쓰는 전통 개념인 십이시진(자시/축시/인시.../해시, 2시간 단위 12구간)으로 아예 개념을 바꿈. UX 개선과 동시에 "실제 이론 반영"이라는 프로젝트 원칙에도 더 부합. 서버로 넘어가는 값도 "자시 (23시~01시)" 같은 텍스트라 AI 프롬프트에 그대로 자연스럽게 반영됨. 관련 헬퍼: `populateSijinSelect`, `SIJIN_OPTIONS` 배열.
- **iOS Safari 자동 확대(zoom) 버그 수정**: `<input>`/`<select>` 폰트 크기가 16px 미만이면 탭할 때 페이지 전체가 확대됐다 축소되는 현상이 있음 → 모든 `.field-row input`, `.field-row select`, `.ymd-row select`의 `font-size`를 14px→16px로 통일. 인풋 `height` 고정값도 `min-height`로 바꿔서 네이티브 렌더링이 안 눌리게 조정.
- **동의 체크박스 UI**: 아래 5번 섹션 참고.
- **OG 썸네일 이미지 제작**: `og-image.png`(1200x630) 신규 제작, 사이트 톤(고서/금테/붉은 인장)에 맞춰 디자인. 메타태그의 `YOUR-DOMAIN-HERE.com`을 실제 배포 도메인(`ai-gwansang-one.vercel.app`)으로 교체 완료.

### 4-6. 결과 공유 링크 — 서버 저장 없이 URL 자체에 결과를 인코딩
- **문제**: 기존엔 공유 버튼이 `window.location.href`(그냥 홈 주소)를 공유해서, 링크 받은 사람이 들어가도 결과가 안 보였음.
- **해결 방식**: 결과 JSON(`title`, `features`, `fortune_score`/`compatibility_score`, `fortune_text`/`summary`)을 `JSON.stringify` 후 **LZ-String 압축**(`compressToEncodedURIComponent`)해서 쿼리스트링에 통째로 실음(`?r=...`는 관상, `?c=...`는 궁합). **서버 API 호출이나 DB 저장 전혀 없음** — URL 자체가 데이터를 담는 그릇 역할. CDN(`cdnjs.cloudflare.com/ajax/libs/lz-string`)에서 라이브러리 로드.
  - [수정 이력] 최초엔 단순 base64로 구현했다가(결과 하나당 URL 약 1100자), 실제 텍스트 분량 기준 압축 없이는 카카오톡이 링크를 인식 못 하는 문제가 발견되어 LZ-String으로 교체 → 약 34% 단축(1141자 → 767자).
- 페이지 로드 시 즉시실행함수(IIFE) `initFromSharedLink()`가 항상 자동 실행되어 쿼리스트링에 `r`/`c`가 있는지 확인 → 있으면 디코딩해서 `showResult`/`showCoupleResult`를 직접 호출(이 두 함수는 API 응답이든 URL 디코딩 데이터든 구분 없이 동일하게 동작하도록 설계됨) → 없으면 아무 것도 안 하고 평소 홈 화면 유지.
- **[치명적 버그, 수정 완료] 궁합 공유 링크가 항상 홈 화면만 보여주던 문제**: `let currentGwansangShareUrl`/`let currentGunghapShareUrl` 변수가 `initFromSharedLink()` IIFE(파일 앞쪽, 즉시 실행됨)보다 코드상 훨씬 뒤에 선언되어 있었음 → JS의 Temporal Dead Zone 규칙상 그 선언 줄이 실행되기 전에는 접근이 금지되는데, IIFE가 실행되는 시점엔 아직 그 선언까지 도달 못 한 상태 → `showCoupleResult` 내부에서 이 변수에 값을 대입하려다 `ReferenceError` 발생 → 마침 `try/catch`로 감싸여 있어 에러가 조용히 삼켜지고 홈 화면으로 빠짐(관상 쪽은 실행 순서상 우연히 덜 걸려서 증상이 약했음). **수정: 두 변수 선언을 `initFromSharedLink()` 정의보다 앞쪽으로 이동**. 이런 구조(파일 위쪽에서 즉시 실행되는 코드가, 파일 아래쪽에서 `let`/`const`로 선언된 값을 참조하는 경우)는 항상 TDZ 에러 위험이 있으니 향후 함수/변수 추가 시 선언 순서 주의할 것.
- 공유 버튼은 `title`/`text` 없이 **`url`만 단독으로 공유**하도록 단순화(이전엔 안내 문구를 URL 앞에 붙여서 공유했는데, 카톡이 텍스트에 섞인 URL을 링크로 인식 못 하고 그냥 통짜 텍스트로 보여주는 문제가 있었음. URL만 단독으로 넘기면 인식률이 크게 개선됨).
- **원본 사진은 공유 링크에 포함 안 됨**(용량 문제 + "사진 자체는 공유 안 한다"는 기존 원칙 유지). 관상 결과의 경우 공유 링크로 들어왔을 때 사진 자리에 🔮 아이콘 placeholder + "원본 사진은 포함되지 않습니다" 안내 문구 표시. 궁합은 애초에 사진이 없어서 이 이슈 자체가 없음.
- "다시 보기"/"다시 입력하기" 버튼 클릭 시 `history.replaceState`로 URL의 공유 파라미터도 함께 정리됨.

### 4-7. 카카오톡 공유 관련 이슈
- **[해결됨] 결과 공유 시 링크가 텍스트 뭉치로 보이던 문제** — 위 4-6 참고 (LZ 압축 + URL 단독 공유로 해결)
- **[해결됨] 궁합 공유 링크가 홈 화면만 보여주던 문제** — 위 4-6 참고 (변수 선언 순서 버그)
- **[미해결] 카톡 인앱 브라우저에서는 공유 버튼이 "복사"로만 동작함**: `navigator.share`(Web Share API)를 카톡 인앱 웹뷰가 지원 안 해서, 코드가 자동으로 폴백(클립보드 복사)하는 것. 모바일 크롬/사파리(진짜 브라우저)에서는 정상적으로 OS 공유시트가 뜸 — 확인 완료.
  - **근본 해결책은 카카오 공유 SDK 연동**(`Kakao.Share.sendDefault()`) — 카카오 디벨로퍼스에서 무료 JS 키 발급 필요. 인앱 브라우저 안에서도 정식 카톡 공유 UI를 띄울 수 있음. **아직 미착수** — 다음 세션 후보 작업.
- **[알아둘 것] 카카오톡 링크 자동 인식 이슈**: 카톡은 `https://` 없이 도메인만 붙여넣으면 링크로 인식 못 하는 경우가 있음(특히 `vercel.app`처럼 하이픈 많은 서브도메인). 공유 시 항상 `https://` 포함해서 보내는 습관 필요. 이건 카톡 클라이언트 자체의 링크 파싱 한계라 코드로 해결 불가.
- **[알아둘 것] 카톡 OG 미리보기 캐시**: 메타태그/이미지를 바꿔도 카톡은 최소 1시간 이상 캐시하고 최대 캐시 기간이 공식적으로 정해져 있지 않음. 강제 갱신하려면 카카오 공유 디버거(`developers.kakao.com/tool/clear/og`)에서 URL 초기화 필요. 캐시 지운 뒤에는 "완전히 새로운 채팅방"에서 테스트해야 확실함(기존 채팅방은 클라이언트 쪽에도 미리보기가 박제돼있을 수 있음).

## 5. 개인정보/법적 고려사항

- **API 호출 자체가 이미 "개인정보 해외이전"**: DB 저장 여부와 무관하게, 사진/생년월일이 Anthropic·Google(미국) 서버로 전송되는 시점에 이미 해당.
  - **[2026-08-24 정정] Anthropic API와 Google Gemini "유료" 등급은 데이터를 학습에 안 쓰지만, Gemini "무료" 등급은 데이터가 Google 제품 개선(학습 포함)에 활용될 수 있음** (ai.google.dev/gemini-api/terms 공식 확인). 이 프로젝트는 현재 Gemini 무료 등급을 기본으로 쓰고 있으므로, 개인정보처리방침에 "무료 등급 이용 시 Google이 데이터를 제품 개선에 활용할 수 있다"는 점을 정확히 명시해야 함 — 기존에 "두 회사 다 학습에 안 씀"이라고 적었던 부분은 부정확했으므로 정정
  - 단기간 자동 삭제(보관기간) 관련 내용은 별도이며, "전송" 자체와 "학습 활용 여부"는 서로 다른 이슈이니 개인정보처리방침에 둘 다 구분해서 명시할 것
- **필요 조치**:
  1. [ ] 개인정보처리방침 페이지 작성 (수집 항목, 목적, 해외이전 사실, **Gemini 무료 등급의 데이터 활용 가능성**, 보관기간=즉시폐기, 파기 등) — 아직 별도 페이지는 없음
  2. [완료] 업로드/입력 전 동의 체크박스 추가 — 관상/궁합 화면 각각에 구현. 문구: "AI 분석을 위해 미국 소재 서버(Anthropic, Google)로 전송되며, 무료 등급 이용 시 Google이 이 데이터를 자사 서비스 개선(AI 학습 포함)에 활용할 수 있음에 동의합니다." 체크 안 하면 분석 버튼이 비활성화되는 방식(하드 게이트). "다시 시도" 시 체크 상태도 초기화됨.
  3. **[알려진 한계]** 이 동의는 서버에 로그로 남지 않음(타임스탬프/기록 없음) — 즉 "동의를 요청하는 절차는 있지만 그 동의가 있었다는 증거는 서버에 안 남는" 상태. 지인 공유 수준 트래픽에서는 합리적인 절충점으로 판단했으나, 트래픽 급증 시 `api/analyze.js`에 최소한의 동의 로그(타임스탬프+consented boolean, PII 제외)를 남기는 것을 고려할 것.
  4. 참고 사례: 이루다 챗봇 사건(2021, 스캐터랩) — 동의 없이 카카오톡 대화를 AI 학습에 재사용해 과징금 1억 330만원 부과. 이 프로젝트와의 핵심 차이: (1) 목적 외 재사용 없음(수집 즉시 AI 분석 용도로만 사용) (2) 서버에 데이터 저장 자체를 안 함(재사용도 유출도 원천 불가) (3) 규모가 하루 캡으로 통제됨. "무료로 널리 퍼뜨리는 것"은 오히려 노출 확대 요인이므로 동의 절차는 유지하는 게 맞다고 판단.
- 리스크 판단: 소규모(지인 공유 수준) 트래픽에서는 실제 문제될 확률 낮음. 다만 트래픽이 예상외로 늘거나 장기 운영 시 위 조치 필요 — **배포 전 반드시 재검토할 것**

## 6. 파일 목록

| 파일 | 역할 |
|---|---|
| `ai-gwansang.html` | 프론트엔드 전체(홈/관상/궁합/결과 화면 포함 단일 파일). `/api/analyze` 프록시 호출 방식. LZ-String CDN 의존성 추가됨(`cdnjs.cloudflare.com/ajax/libs/lz-string`) |
| `api-analyze.js` | 배포용 서버 프록시(Vercel Serverless Function). Gemini 우선/Claude 폴백, 하루 호출 캡, type별(gwansang/gunghap) 라우팅. 배포 시 `api/analyze.js` 경로에 위치해야 함 |
| `vercel.json` | 루트(`/`) 요청을 `ai-gwansang.html`로 라우팅하는 rewrite 규칙. 파일명을 `index.html`로 바꾸지 않고 그대로 유지하기 위한 설정 |
| `og-image.png` | 카톡/SNS 공유 시 노출되는 썸네일 이미지(1200x630). 리포 루트에 위치 |
| `CLAUDE.md` | 이 문서 |

## 6-1. 배포 현황

- **배포 완료**: `https://ai-gwansang-one.vercel.app` (Vercel + GitHub 연동, push하면 자동 재배포)
- GitHub 리포는 Claude Code CLI로 생성/push 완료
- 루트 접속 시 404 나던 문제 → `vercel.json` rewrite로 해결(파일명은 `ai-gwansang.html` 그대로 유지)
- 환경변수(`ANTHROPIC_API_KEY`, `GEMINI_API_KEY`)는 "Production and Preview" 환경에 등록 완료

## 7. 다음 할 일 (다음 세션에서 이어갈 것)

- [x] `ai-gwansang.html`의 fetch 주소를 `/api/analyze`로 교체
- [x] Vercel 프로젝트 생성 + 환경변수 등록 + 배포
- [x] OG 이미지 제작 및 실제 도메인으로 메타태그 교체
- [x] 동의 체크박스 추가 (개인정보처리방침 페이지 자체는 아직 없음)
- [x] 생년월일 입력 UX 개선 (드롭다운 방식)
- [x] 태어난 시각 입력 UX 개선 (십이시진 드롭다운 방식)
- [x] 결과 공유 링크가 실제 결과를 보여주도록 개선 (URL 인코딩 방식, LZ-String 압축)
- [x] 궁합 공유 링크가 홈 화면만 뜨던 버그 수정 (변수 선언 순서/TDZ 이슈)
- [x] 공유 링크가 카톡에서 텍스트 뭉치로 보이던 문제 개선 (URL 단독 공유 + 압축)
- [ ] **카카오 공유 SDK 연동** — 카톡 인앱 브라우저에서도 정식 공유 UI 뜨게 하기 (다음 세션 최우선 후보 작업)
- [ ] 개인정보처리방침 페이지 실제 작성
- [ ] Anthropic/Gemini 콘솔에 지출 하드리밋 최종 확인 (자동충전 꺼짐 여부, Gemini는 결제수단 연결 여부에 따라 무료 등급 유지 가능)
- [ ] (선택) Vercel KV 연결해서 하루 카운터를 정확하게 영속화 — 현재는 메모리 변수라 서버리스 인스턴스 재시작/다중화 시 캡이 무력화될 수 있음(바이럴 시 리스크)
- [ ] (선택) 커스텀 도메인 연결 여부 결정 — Vercel 자체는 무료, 도메인 구매 비용만 별도
- [ ] (선택, 낮은 우선순위) 광고 붙이는 결정을 하게 되면 Vercel Pro 전환 필요

## 8. 진행 방식 메모

- 반말 사용, 간결하고 직접적인 응답 선호 (사용자 커뮤니케이션 스타일)
- 코드 수정은 큰 구조변경 시 패치 누적보다 전체 재작성이 안전함을 확인(1차 궁합기능 추가 중 패치 누적으로 문법오류 발생했던 경험 있음)
- 배포/비용/법적 이슈 등 실무적 질문이 나올 때마다 검색해서 최신 정보 확인 후 답변(모델 가격, Vercel 요금제, 개인정보보호법 등 시점에 따라 바뀌는 정보이므로)
- 개발 배경지식(React/Next.js 차이, 쿼리스트링 동작 원리, IIFE 등)을 궁금해하며 원리 이해를 중요하게 여김 — 코드만 던지지 말고 "왜 이렇게 동작하는지" 구조적으로 설명하는 것을 선호
- 배포/디버깅은 Claude Code CLI로 직접 진행(로컬 git/GitHub 인증 활용), Claude와의 대화창에서는 파일 수정 + 안내만 하고 실제 git 명령은 사용자가 CLI에서 실행하는 방식으로 진행 중
- UI 변경 제안 시 한 번에 여러 가지를 밀어붙이기보다, 사용자가 "이 방향 별로다" 피드백 주면 즉시 원래 방식으로 되돌리는 것을 선호(예: 사주궁합 폼 인라인 레이아웃 시도 후 다시 세로 스택으로 롤백한 사례 있음)
